# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Endpoint REST de reconocimiento de placas (proporcionado por el docente).
# NUNCA se debe escribir la URL real (con su ?code=...) directamente en el
# código ni subirla al repositorio: solo debe existir como variable de
# entorno local (.env, que está en .gitignore) y como secreto de GitHub
# Actions / Azure en producción.
VITE_OCR_ENDPOINT=
